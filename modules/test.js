const Carplay = require('./Carplay')
const config = {
    dpi: 160,
    nightMode: 0,
    hand: 0,
    boxName: 'nodePlay',
    width: 800,
    height: 600,
    fps: 20,

}
const carplay = new Carplay(config)